/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,js}"],
  fontFamily: {
    'nunito': ['nunito', 'sans-serif'],
    'base': ['"Anonymous Pro"', 'serif'] // Ensure fonts with spaces have " " surrounding it.
  },
  plugins: [],
}